NEXT_PUBLIC_CRM_API_URL=http://127.0.0.1:4000
# QA Lab page: http://localhost:4001/qa — requires crm-api with CRM_QA_LAB=true and `npm run seed:qa` in apps/crm-api
# JWT for Socket.io when crm-api has CRM_REQUIRE_SOCKET_AUTH=true (sign with CRM_JWT_SECRET, claim sub = user id)
# NEXT_PUBLIC_CRM_SOCKET_TOKEN=
