# Myle-Dashboard-main-3 — `templates/` (reference snapshot)

Jinja2 HTML from the legacy Flask app (**`base.html`**, dashboard, working views, admin, wallet, Day 2, intelligence, etc.). **74** template files — full server-rendered UI surface.

vl2 replaces this with **`frontend/src`** (React + React Router). Route/API mapping stays in **`../README.md`** (*`routes/`*) and **`app/api/v1/*`**. Templates here are **read-only reference** when porting copy or behavior.
